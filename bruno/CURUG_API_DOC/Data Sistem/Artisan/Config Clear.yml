info:
  name: Config Clear
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/artisan/cache-clear"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
