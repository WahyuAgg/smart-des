info:
  name: Storage Link
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/artisan/storage-link"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
