info:
  name: Perangkat Desa by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/test-model/perangkat-desa/{{perangkat_desa_id}}"
  auth: inherit

runtime:
  variables:
    - name: perangkat_desa_id
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
