info:
  name: Penduduk by ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/test-model/penduduk/{{penduduk_id}}"
  auth: inherit

runtime:
  variables:
    - name: penduduk_id
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
