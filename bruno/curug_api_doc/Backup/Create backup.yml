info:
  name: Create backup
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/backup"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
