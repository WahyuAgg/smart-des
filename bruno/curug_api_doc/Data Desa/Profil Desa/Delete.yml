info:
  name: Delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/api/ref-profil-desa"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
