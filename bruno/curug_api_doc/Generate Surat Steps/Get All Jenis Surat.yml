info:
  name: Get All Jenis Surat
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/srt-jenis-surat"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
