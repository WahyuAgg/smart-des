info:
  name: Optimize Clear
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/api/artisan/optimize-clear"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
