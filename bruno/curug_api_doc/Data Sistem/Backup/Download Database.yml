info:
  name: Download Database
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/backup/download-sqlite"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
