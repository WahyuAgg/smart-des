info:
  name: Get by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/kk/{{id_kk}}"
  auth: inherit

runtime:
  variables:
    - name: id_kk
      value: "46"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
