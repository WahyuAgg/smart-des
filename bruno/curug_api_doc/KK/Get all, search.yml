info:
  name: Get all, search
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/kk?per_page=2"
  params:
    - name: per_page
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
